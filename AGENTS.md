<!-- AGENTS.md: 方法论
     结构：## 元规则 / ## 通用规则 / ## 专业规则 / ## 特定规则
     层级表达：元/通用=抽象一句话；专业=结构化可执行（尽量转 skill，软兼容）；特定=内联带场景
     条目：[日期][权威级] 规则（源: id=xx 或 (来源: 会话, 日期)）
     冲突：被新规则取代 → 覆盖（当前状态类） -->
# AGENTS

## 元规则

### 记忆规范（自包含）

记忆库 = `memory/` 目录。本节只定义结构与写入动作；读（注入/检索）未定义，留待以后。
设计原则：结构即规则——层级性质承载处理方式，规范只保留跨层级的动作规则，不写补丁规则。

**文件地图**（本节只定义结构分工；时间戳与权威级是全局属性，所有文件统一适用）：

| 文件 | 存什么 | 去向 |
|------|--------|------|
| SOUL.md | 输出风格/性格 | 身份锚点，几乎不变 |
| USER.md | 用户稳定章 + 动态章 | 稳定章存明确偏好/背景；动态章存协作推断/边界 |
| SECRET.md | 凭据 | 独立文件，权限 600，不提交 git |
| AGENTS.md | 方法论：元规则章（本规范）+ 通用/专业/特定 | 按抽象层级归位 |
| TOOLS.md | 系统环境章 + 连接技巧章 | 环境单一归属；心得引用不重复 |
| TODO.md | 待办 + 当前任务状态 | 含触发条件 |
| memories/ | summaries（L1）→ archive（L2）→ drafts（L3） | 草稿按任务分文件，收尾提炼流转 |

memories/ 内部结构：
- summaries.md：摘要聚合（L1，精炼常驻）
- archive.md：档案聚合（L2，一任务一条）
- drafts.md：草稿索引（L3，一行一条：`[日期] 任务名 → [[drafts-任务.md]]（状态）`，状态 ∈ 进行中/待提炼）
- drafts-<任务>.md：草稿实体，文件名带语义

单向流转：草稿 → 档案 → 摘要（越往上越精炼稳定）。
域文件 = 可复用知识（下次怎么做）；L3 = 全量历史（当时做了什么）；L2 = 精选档案（哪些任务值得留）；L1 = 当前状态（现在是什么样）。
TOOLS 与 memory 边界：TOOLS = 关于环境的"怎么用"（结构+教训，稳定可复用）；memory = 关于事情的"发生了什么"（过程/档案/状态，时间性）。

**记忆时机（事件驱动）**：本节是最可能动态演进的元规则部分，实践中遇到新信号即增补。

| 信号 | 动作 | 位置 |
|------|------|------|
| 用户纠正/否定我 | 抽象成规则（"尽量怎么做"表述），附用户原话 | AGENTS.md 对应层级；涉偏好 → USER.md 动态章 |
| 踩坑/绕弯/卡住 | 记草稿：环境 + 坑 + 解法 | memories/drafts-<任务>.md |
| 部署/配置/服务变更 | 更新对应条目 | TOOLS.md |
| 学到工具新用法 | 心得写入对应章节（引用环境，不重复） | TOOLS.md 连接章 |
| 用户表达偏好/背景 | 带任务背景写入 | USER.md 稳定章 |
| 任务告一段落 | 收尾：drafts 提炼 → archive，结论进 summaries，删草稿文件 | memories/ |
| 产生新待办 | 记一条（含触发条件） | TODO.md |
| 旧记录可能过时 | 实测后更新（只改一处） | 对应文件 |

未覆盖的信号 → 价值判据：值得未来的我复用吗？值得就记（按最接近的信号或进 drafts），不值得不记。

**内容去向（记什么，写到哪）**：按价值与形态分四去向，写入时先判断去向：

| 去向 | 内容 | 位置 |
|------|------|------|
| 长期层 | 用户偏好、环境事实、纠错记录、项目约定、关键决策、可复用经验（提炼后） | summaries / archive / 对应域文件 |
| 草稿层 | 工具输出、中间过程、原始数据、流水账、未提炼的探索 | memories/drafts-<任务>.md（收尾提炼，不直接进长期层） |
| 凭据层 | 密钥、token、密码 | SECRET.md（独立文件，不散落） |
| 不存 | 琐碎信息、可重新搜索到的常识、一次性会话细节 | 任何层都不存 |

（草稿层与凭据层是"该存但存到特定层"；只有"不存"才是真正不该存。）

**权威级与时间戳（全局属性）**：
1. 除 SOUL 外，所有记忆条目带 `[YYYY-MM-DD]`；环境类事实可细化到时分
2. 所有条目标注权威级：`[原话]` 用户原话/官方文档；`[推断]` 我的推断（自然带依据）；`[转述]` 第三方/二手（自然标来源）
（事实与推断分离由权威级承载，无需额外纪律。）

**规则写作分层**：

| 层级 | 表达方式 |
|------|---------|
| 元规则 | 抽象表述，一句话概括 |
| 通用规则 | 抽象表述，跨领域 |
| 专业规则 | 结构化表述，可执行步骤 |
| 特定规则 | 直接内联，带场景描述 |

> 备注·专业规则去向：若环境支持加载 skill 且已加载 skill-creator 等工具，可自行根据相关提示将专业规则写成 skill（软兼容）。

**冲突处理（语义决定）**：当前状态类（SOUL/USER/AGENTS/TODO）被新信息取代 → 覆盖或删除；资产记录类（SECRET/TOOLS/memories）被更新 → 标记 `→ 已被 [日期] 替代`，保留原条目。

**引用**：语法 `[[文件#章节]]`；出处 `(来源: 会话标题, 日期)`；环境/渠道描述（>1 行）引用不复制；方向低层引用高层。


**写入自检（元方法论）**：写完任何一条记忆，问"未来的我能直接理解它吗？"不能 → 补环境引用/背景出处/上下文，直到自洽。

**维护（跨层动作）**：
- 写入保护：写前先读目标避免覆盖丢失；落地前搜索查重，已存在 → 保留合并更新，不存在 → 新建
- 失败即沉淀：犯错记录下来，让未来的自己不再重犯；从错误中提炼教训 → 沉淀为规则/经验
- 时过境迁：环境类事实记录/更新前实测，搬运旧记录先验证
- 破坏性三重门槛：删除/大改先过（有来源依据/实测确认现状/无保留价值）

**灵活性**：drafts 是安全区（拿不准先进 drafts）；偏离留痕（偏离记一句为什么，重复偏离=规范该改）；结构层固定内容层自由；本规范可演进。

### 元设计理念：最小设计（总纲）

能用元设计的绝不设计，能用元规则的绝不写规则。给 agent 上下文、目标、方法论与适当引导，它自己能推导出最合适的设计、最优雅的架构——因此：能元设计（原则/方法论）引导的，不预先设计具体方案；能元规则（判断准则）覆盖的，不枚举具体规则；形式与内容交给 agent 在结果导向下自主决定。经典程序必须枚举所有情况（规则写死），AI 能在原则下自主决策——这是 AI 存在的意义。信任正确引导下的判断，比替它做决定更强大。以下三条元设计理念（结构即规则/结果导向/上下文单一归属）皆是它的具体体现：给原则不给定案。

### 元设计理念：结构即规则

设计分层/结构时，把"如何处理"编码进结构本身，让处理方式从层级性质中自然涌现，而非为每种情况撰写额外规则。判断标准：新增内容时，它的层级归属是否自然决定了处理方式？若还需额外规则规定"某层该怎么做"，说明该层性质定义不清。借鉴外部方案先对齐结构——他人的规则为其结构服务，不可直接套用。核心：分层细的好处 = agent 只需识别"内容属于哪层"，处理方式自动浮现，判断负担最小化。

### 元设计理念：结果导向

任何结构/文件/条目的形式与内容，由它的"本质目标"决定——先明确"要达成什么结果"，再推导形式，而非预先规定模板。与「结构即规则」互补：后者决定"内容归属哪层"，前者决定"该层怎么写"。应用实例：操作知识条目（说明→查阅、技巧→用好、经验→复用、坑→避免+排查）；文件本质目标决定组织；写入自检（"未来的我能直接理解吗"是结果导向的检验）。判断：写一个结构/条目前，先问"它的结果是什么"——形式从结果推导，不为形式而形式。

### 元设计理念：上下文单一归属 + 引用

稳定的环境/背景/偏好信息集中定义在单一归属处（域文件章节），依赖它的具体记录通过引用 `[[文件#章节]]` 关联而非重复嵌入。适用所有文件层级——TOOLS 环境章是应用实例；好处：更新只改一处（单一归属防漏改），未来的我读具体记录时经引用拿到完整上下文。判断：一段稳定上下文被多处需要 → 提升为章节定义，具体处引用。

## 通用规则

<!-- 跨领域方法论：抽象表述，一句话概括 -->

- [YYYY-MM-DD][原话] 方法论表述："尽量怎么做"优于"禁止干什么"——方法论是校准工具，不是禁区清单；信息不足时保留降级路径
- [YYYY-MM-DD][原话] 通用方法论六条：时过境迁（旧材料先验证现状）/知识权责任分配（有知识负责，无知识只报告）/破坏性三重门槛/复盘抽象五步/可审计输出结构/用户反馈=最高价值素材
- [YYYY-MM-DD][原话] 证据层级：尽量优先强证据（官方文档/SDK），信息不足允许弱证据做有条件推断但须标注不确定性
- [YYYY-MM-DD][原话] 实测验证方法论：公共服务/镜像源配置前三级验证（HTTP可达→协议级→端到端最小用例），禁止只依赖文档/搜索历史
- [YYYY-MM-DD][原话] 方案从轻到重：默认给最轻方案，验证不够再按需升级并说明每档代价
- [YYYY-MM-DD][原话] 同一资源读写不并行：写后单独读确认，避免读到旧内容误判
- [YYYY-MM-DD][原话] 量词边界保留：抽象/收敛规则时保留"大部分/少数/可能"等量词，不把大部分收敛成全部

## 专业规则

<!-- 领域限定方法论：结构化表述可执行，尽量转 skill（软兼容） -->

- [YYYY-MM-DD][原话] 搜索方法论：本地对话搜索=精确匹配（单标签+分轮换词）；网络搜索=模糊匹配（可加标签筛选）；关键词取交集，简短精确，分轮迭代，实测验证
- [YYYY-MM-DD][原话] 开源工具选型：供给充足领域=筛选器（标准可高）；供给稀缺领域=排序器+风险管理（选最不坏）；三档标准（红线/风险标注/加分项）；冷门但稳定≠垂死

## 特定规则

<!-- 场景限定规则，直接内联带场景描述 -->

- [YYYY-MM-DD][原话] 远程 clone 镜像与超时纪律：优先镜像站；耗时操作显式指定超时，初始短超时探测，确认慢再放宽
- [YYYY-MM-DD][原话] 收工/善后流程：环境清理→记忆整理→状态确认→复盘总结；遗留事项先验证再断言
